const signUp = async (name, email, password) => {
    return;
}

const signIn = async (email, password) => {
    return;
}

const signOut = async () => {
    return;
}

export { signUp, signIn, signOut }